name: trip_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 📦 حزم المشروع
  get: ^4.6.6
  google_maps_flutter: ^2.5.0
  http: ^1.1.0
  get_storage: ^2.1.1 # ✅ لحفظ الجلسة

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true